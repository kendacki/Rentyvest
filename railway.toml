# Repo-root Railway config (used when Root Directory is NOT set).
# Recommended: set Root Directory to services/core-api instead and use /services/core-api/railway.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
