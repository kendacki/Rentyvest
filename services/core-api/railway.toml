# Railway config for core-api.
# In the Railway dashboard, set Service → Settings → Root Directory = services/core-api
# (Config path from repo root: /services/core-api/railway.toml)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
